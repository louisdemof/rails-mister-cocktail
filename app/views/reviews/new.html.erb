<div class="align">
  <h1 class="menu-top">Add a review</h1>
  <%= simple_form_for [@cocktail, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit , class: "btn-flat"%>
  <% end %>
</div>
